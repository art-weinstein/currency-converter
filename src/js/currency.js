export default class chooseCurrency {
  static chosenCurrency (){
      $(document).ready(function() {
      $('#choose').click(function(currency) {
        let currency = $('#currency').val();
      });
    });
  }
}
